version: '3.8'

services:
  api:
    build: .
    container_name: api_sensores_servicio
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
    - DB_HOST=host.docker.internal
    restart: unless-stopped
